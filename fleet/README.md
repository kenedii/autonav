# Fleet

Fleet-level orchestration code lives under `fleet_management_app/`.

- `fleet_management_app/client_api/`: Software that runs on each car (Jetson side).
- `fleet_management_app/host_app/`: Central host service and frontend for monitoring/managing cars.

Start with `fleet_management_app/README.md` for full setup and run instructions.
